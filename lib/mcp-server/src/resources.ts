import type { McpServer } from "@modelcontextprotocol/server";
import type { DataStore } from "@workspace/nlp-runner";

export function registerResources(_server: McpServer, _store: DataStore): void {
  // Resources are registered in tools.ts alongside the tools they complement.
}
